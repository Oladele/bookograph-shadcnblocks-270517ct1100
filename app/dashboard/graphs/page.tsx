/**
 * Graphs route (`/dashboard/graphs`). Rendered inside Sidebar1 via `app/dashboard/layout.tsx`.
 */
export default function GraphsPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Graphs</h1>
      <p className="mt-2 text-muted-foreground">Graphs placeholder page.</p>
    </div>
  );
}
