/**
 * Dashboard home route (`/dashboard`). Rendered inside Sidebar1 via `app/dashboard/layout.tsx`.
 */
export default function HomePage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold tracking-tight">Home</h1>
      <p className="mt-2 text-muted-foreground">Home placeholder page.</p>
    </div>
  );
}
